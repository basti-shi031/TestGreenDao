package cn.com.workshop7.factory.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.boateng17.testgreendao.entity.Car;

import com.example.boateng17.testgreendao.entity.Color;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COLOR".
*/
public class ColorDao extends AbstractDao<Color, Long> {

    public static final String TABLENAME = "COLOR";

    /**
     * Properties of entity Color.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property R = new Property(1, int.class, "R", false, "R");
        public final static Property G = new Property(2, int.class, "G", false, "G");
        public final static Property B = new Property(3, int.class, "B", false, "B");
        public final static Property Car = new Property(4, Long.class, "car", false, "CAR");
    };

    private DaoSession daoSession;


    public ColorDao(DaoConfig config) {
        super(config);
    }
    
    public ColorDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COLOR\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"R\" INTEGER NOT NULL ," + // 1: R
                "\"G\" INTEGER NOT NULL ," + // 2: G
                "\"B\" INTEGER NOT NULL ," + // 3: B
                "\"CAR\" INTEGER);"); // 4: car
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COLOR\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Color entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getR());
        stmt.bindLong(3, entity.getG());
        stmt.bindLong(4, entity.getB());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Color entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getR());
        stmt.bindLong(3, entity.getG());
        stmt.bindLong(4, entity.getB());
    }

    @Override
    protected final void attachEntity(Color entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Color readEntity(Cursor cursor, int offset) {
        Color entity = new Color( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // R
            cursor.getInt(offset + 2), // G
            cursor.getInt(offset + 3) // B
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Color entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setR(cursor.getInt(offset + 1));
        entity.setG(cursor.getInt(offset + 2));
        entity.setB(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Color entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Color entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getCarDao().getAllColumns());
            builder.append(" FROM COLOR T");
            builder.append(" LEFT JOIN CAR T0 ON T.\"CAR\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Color loadCurrentDeep(Cursor cursor, boolean lock) {
        Color entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Car car = loadCurrentOther(daoSession.getCarDao(), cursor, offset);
        entity.setCar(car);

        return entity;    
    }

    public Color loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Color> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Color> list = new ArrayList<Color>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Color> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Color> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
